Options -Indexes

<FilesMatch "^(bootstrap|config|midtrans|openai|ai-provider)\.php$">
  Require all denied
</FilesMatch>

<IfModule mod_headers.c>
  Header always set Cache-Control "no-store"
  Header always set X-Content-Type-Options "nosniff"
</IfModule>
